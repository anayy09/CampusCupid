import { render, screen } from "@testing-library/react";
import LandingPage from "./LandingPage";

test("renders welcome message", () => {
  render(<LandingPage />);
  expect(screen.getByText(/welcome to campus cupid/i)).toBeInTheDocument();
});
